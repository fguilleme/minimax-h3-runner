[project]
name = "minimax-h3-runner"
version = "0.1.0"
description = "Headless staged MiniMax H3 runner for ROCm"
requires-python = ">=3.11"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
